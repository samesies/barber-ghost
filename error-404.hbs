{{!--
This error template is used for all 404 errors, which might occur on your site.
It's a good idea to keep this template as minimal as possible in terms of both file size and complexity.
--}}

<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>{{meta_title}}</title>
  
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Cabin:400,700">
  <link rel="stylesheet" href="{{asset "css/fonts.min.css"}}">
  <link rel="stylesheet" href="{{asset "css/app.min.css"}}">
</head>
<body class="error-template">
  {{> header}}

  <main role="main" class="💈">
    <section class="mast section-padding">
      <div class="grid-small">
        <h2 class="color-alpha">{{code}}</h2>
        <p class="mast__text">The page does not exist.</p>
      </div>
    </section>
  </main>

  <footer class="footer section-padding">
    <div class="grid">
      {{#if @labs.subscribers}}
        <div class="subscribe" id="subscribe">
          <div class="subscribe__container">
            <span class="subscribe__title">Subscribe</span>
            <p class="subscribe__text">Get a weekly email of posts I’ve added to the site.</p>
            {{subscribe_form placeholder="Email Address"}}
          </div>
        </div>

        <hr class="sep"/>
      {{/if}}

      <div class="footer__container">
        <p class="footer__text">New posts added every few days.</p>

        {{#get "tags" limit="5" order="count.posts DESC"}}
          {{#if tags}}
            <ul class="footer__tags">
              {{#foreach tags}}
                <li>
                  <a class="footer__link" href="{{url}}">{{name}}</a>
                </li>
              {{/foreach}}
            </ul>
          {{/if}}
        {{/get}}

        {{> social}}
      </div>
    </div>
  </footer>
</body>
</html>
