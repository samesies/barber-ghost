<article class="posts__post">
  <a class="posts__link" href="{{url}}">
    {{#if feature_image}}
      <figure class="posts__img">
        <img src="{{feature_image}}" alt="{{title}}" data-aos="fade-in"/>
      </figure>
    {{/if}}
    <div>
      <h2 class="posts__text"><strong>{{title}}</strong> — {{excerpt words="35"}}</h2>
    </div>
  </a>
</article>
